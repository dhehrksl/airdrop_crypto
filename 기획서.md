# [기획서] AI 자동화 기반 가상화폐 에어드랍 정보 큐레이션 플랫폼

---

## 1. 프로젝트 개요

**프로젝트 목적**
파편화된 가상화폐 에어드랍 정보를 자동으로 수집하고, AI를 통해 스캠을 필터링 및 규격화하여 사용자에게 안전하고 정제된 정보만을 제공하는 모바일 애플리케이션 구축.

**타겟 유저**
매일 에어드랍 및 바운티 작업에 참여하며, 스캠 정보에 피로도를 느끼는 가상화폐 투자자.

**개발 환경 및 기술 스택**
*   **프론트엔드:** React Native, Expo (안드로이드 타겟 최적화)
*   **백엔드:** Node.js, Express
*   **데이터베이스:** MongoDB
*   **AI API:** Gemini 1.5 Flash (데이터 파싱 및 스캠 필터링)
*   **수집 도구:** Axios, Cheerio, Node-Telegram-Bot-API

---

## 2. 시스템 아키텍처 및 파이프라인

전체 프로세스는 사람의 개입 없이 100% 자동화로 동작합니다.

1.  **Data Collector:** Node.js 크롤러가 타겟 채널(텔레그램, 웹사이트)에서 1시간 주기로 Raw Data 수집.
2.  **AI Gatekeeper:** 수집된 텍스트를 Gemini API로 전송.
3.  **Data Processing:** Gemini가 스캠 여부 판별(점수화) 및 JSON 포맷으로 필수 데이터 추출.
4.  **Database Storage:** 신뢰도 점수가 기준치(예: 80점) 이상인 정상 데이터만 MongoDB에 적재.
5.  **Client App:** Expo로 빌드된 모바일 앱에서 DB 데이터를 호출하여 피드(Feed) 형태로 렌더링.

---

## 3. 기능 요구사항 명세

### 3.1 백엔드 파트 (데이터 파이프라인)

*   **크롤링 스케줄러:** `node-cron` 등을 활용하여 정해진 주기마다 타겟 소스의 최신 게시글 50건 수집.
*   **중복 방지 로직:** 게시글의 고유 URL 또는 메시지 ID를 해시 처리하여 기존 DB와 대조, 중복 수집 방지.
*   **AI 프롬프트 엔지니어링:** Gemini API 호출 시 `response_mime_type: "application/json"`을 강제하여 응답 포맷 고정.
*   **스캠 필터링 기준:** 지갑 개인키(Private Key) 요구, 사전 입금(Deposit) 요구, 비공식 단축 URL 포함 시 신뢰도 점수 대폭 하향 처리.

### 3.2 프론트엔드 파트 (모바일 앱)

*   **홈 피드 (Home Feed):** 최신순/마감임박순 정렬 기능이 포함된 무한 스크롤 리스트 뷰.
*   **상세 페이지 (Detail View):** 에어드랍 참여 방법 3줄 요약, 보상 규모, 공식 링크 제공.
*   **푸시 알림 (Push Notification):** '초고수익 예상' 또는 '마감 1일 전' 등의 조건부 푸시 알림 발송.
*   **인앱 브라우저 (In-App Browser):** 참여 링크 클릭 시 외부 브라우저 이탈 없이 앱 내부에서 웹 뷰를 띄워 체류 시간 확보.

---

## 4. 데이터베이스 스키마 (MongoDB 예시)

| 필드명 | 타입 | 설명 |
| :--- | :--- | :--- |
| `_id` | ObjectId | 고유 식별자 |
| `title` | String | 프로젝트명 및 에어드랍 제목 |
| `description` | String | Gemini가 3줄 요약한 참여 방법 |
| `official_link` | String | 참여할 수 있는 공식 URL |
| `end_date` | Date | 에어드랍 마감 일자 |
| `trust_score` | Number | Gemini가 평가한 신뢰도 점수 (0~100) |
| `source` | String | 데이터 출처 (예: Telegram - @AirdropChannel) |
| `created_at` | Date | DB 적재 시간 |

---

## 5. 상세 예외 처리 및 엣지 케이스 방어 로직

서비스의 안정성을 결정짓는 가장 중요한 파트입니다. 각 구간별 발생 가능한 예외 상황과 대처 방안을 명세합니다.

### 5.1 수집기(크롤러) 예외 처리

*   **타겟 웹사이트 DOM 구조 변경:** 데이터 수집 값이 `null`이거나 빈 문자열일 경우, DB 저장을 즉시 중단하고 관리자에게 웹훅(Slack/Discord) 에러 알림 발송.
*   **IP 차단 (Rate Limit):** 대상 서버에서 요청을 거부(HTTP 429 Too Many Requests)할 경우, 지수 백오프(Exponential Backoff) 알고리즘을 적용하여 재시도 간격을 점진적으로 늘림.
*   **텔레그램 채널 폭파 및 비공개 전환:** API 응답 에러 발생 시 해당 소스를 일시적으로 수집 타겟에서 제외(Disable) 처리.

### 5.2 AI (Gemini API) 예외 처리

*   **API 응답 시간 초과 (Timeout):** 요청 후 15초 이상 응답이 없으면 해당 데이터 처리를 큐(Queue)의 맨 뒤로 미루고 다음 데이터 먼저 처리.
*   **JSON 포맷 파괴 (Hallucination):** Gemini가 지정한 JSON 형식을 무시하고 일반 텍스트나 깨진 괄호를 반환할 경우, 정규식(`try-catch` 블록 내 `JSON.parse`)으로 1차 복구 시도. 복구 실패 시 해당 데이터 폐기(Drop).
*   **필수 데이터 누락:** 신뢰도 점수는 90점이나 `official_link` 값이 빈 문자열로 반환될 경우, 앱 노출 시 심각한 결함이 되므로 DB 적재 프로세스에서 강제 필터링(Drop).

### 5.3 서버 및 DB 예외 처리

*   **데이터 중복 적재 충돌:** 동일한 에어드랍 정보가 다른 소스에서 수집될 경우, 고유 인덱스(Unique Index) 위반 에러가 발생. 이 경우 `upsert` 로직으로 전환하여 기존 데이터의 '출처(Source)' 정보만 배열에 추가.
*   **트래픽 스파이크:** 푸시 알림 발송 직후 다수의 유저가 동시 접속하여 DB 커넥션 풀(Connection Pool)이 고갈될 경우, 리드 레플리카(Read Replica)를 활용하거나 Redis 캐싱 계층을 두어 부하 분산.

### 5.4 클라이언트(모바일 앱) 예외 처리

*   **네트워크 단절 (Offline):** 유저의 데이터 통신이 끊겼을 때 빈 화면을 노출하지 않도록, 이전에 불러온 1 페이지 분량의 데이터를 로컬 스토리지에 캐싱하여 렌더링.
*   **무한 로딩 버그 (Spinner Lock):** 백엔드 응답이 지연될 때 로딩 스피너가 영원히 도는 것을 방지하기 위해 10초 타임아웃 설정. 10초 경과 시 "정보를 불러오는 중 문제가 발생했습니다. 다시 시도해 주세요" 안내 문구 및 새로고침 버튼 렌더링.
*   **외부 링크 접속 불가 (HTTP 404):** 유저가 '참여하기' 버튼을 눌렀으나 대상 사이트가 닫힌 경우, 앱 내부에서 에러를 핸들링하여 "현재 해당 프로젝트 웹사이트에 접근할 수 없습니다"라는 토스트 알림 노출.

---

## 6. 수익화(광고) 배치 전략

안드로이드 기반 구글 애드몹(AdMob) 연동 기준입니다.

*   **하단 배너 광고 (Banner):** 메인 피드와 상세 페이지 하단에 항상 고정 노출하여 기본 노출수(Impression) 보장.
*   **피드 네이티브 광고 (Native):** 리스트 항목 7개당 1개 꼴로 앱의 UI와 완벽하게 일치하는 디자인의 네이티브 광고 삽입 (유저 경험 해침 최소화).
*   **보상형 동영상 (Rewarded):** '신뢰도 95점 이상 초고수익 예상 에어드랍' 정보 열람 시, 15초 광고 시청 후 잠금 해제하는 방식으로 객단가(eCPM) 극대화.

---

## 7. 구현 현황 (2026-05-08 기준)

### 7.1 실행 환경

| 서비스 | 위치 | 포트 | 비고 |
| :--- | :--- | :--- | :--- |
| MongoDB | 로컬 | 27017 | 단일 노드 |
| Backend (Express) | `backend/` | 4000 | `npm start`, `USE_MOCK=true` 기본 |
| Admin (Vite + React) | `admin/` | 5173 | `Bearer ADMIN_TOKEN` 인증 |
| Mobile (Expo Metro) | `frontend/` | 8081 | `npx expo start --lan`, SDK 54 |

폰에서 Expo Go로 접속하려면 `frontend/app.json`의 `extra.API_BASE_URL`을 PC의 LAN IP(예: `http://192.168.55.143:4000`)로 설정하고, 폰을 같은 Wi-Fi에 연결한 뒤 `exp://<LAN-IP>:8081`을 Expo Go에 입력하거나 QR을 스캔.

### 7.2 Expo SDK 51 → 54 마이그레이션

폰에 설치된 Expo Go가 SDK 54용이라 프로젝트도 함께 업그레이드.

* 핵심 의존성: `expo` `^54.0.34`, `react` `19.1.0`, `react-native` `0.81.5`, `@types/react` `~19.1.10`, `typescript` `~5.9.2`
* Expo 모듈: `expo-constants` `~18.0.13`, `expo-device` `~8.0.10`, `expo-notifications` `~0.32.17`, `expo-status-bar` `~3.0.9`
* peer-dep 충돌 회피를 위해 `frontend/.npmrc`에 `legacy-peer-deps=true` 추가
* `npx expo install --fix`가 빠뜨린 `babel-preset-expo`를 명시적 devDependency로 추가
* Node 권장 버전 ≥ 20.19.4 (현재 환경은 v20.13.1 — Metro 0.83.x에서 EBADENGINE 경고가 뜨지만 동작은 함)

### 7.3 sentry-expo 제거

`sentry-expo`는 RN 0.81에서 `promise/setimmediate/es6-extensions` 모듈을 해결하지 못해 번들이 깨지며, 이미 deprecated 상태. `SENTRY_DSN`이 비어 있어 실효 동작도 없었으므로 다음과 같이 정리:

* `sentry-expo`, `@sentry/react-native` 패키지 제거
* `frontend/src/utils/sentry.ts`를 no-op stub으로 교체 (`initSentry`, `captureException`이 빈 함수)
* `app.json` plugins 에서 `sentry-expo` 제거

향후 정식 도입 시 `@sentry/react-native`로 직접 세팅 권장.

### 7.4 웹 플랫폼 지원 추가

PC 브라우저(`expo start --web`)에서도 동작하도록 의존성 추가:

* `react-native-web`, `react-dom`, `@expo/metro-runtime`

웹 환경 대응 패치 (`DetailScreen.handleParticipate`):

* `Platform.OS === 'web'`이면 `Linking.canOpenURL` 검사 우회 후 `window.open(url, '_blank')`으로 새 탭에 외부 사이트 오픈. 이전에는 RN-Web의 `canOpenURL`이 false를 반환해 "현재 해당 프로젝트 웹사이트에 접근할 수 없습니다" 메시지가 무조건 떴음.
* 모바일에서는 기존대로 `Linking.canOpenURL` 후 in-app `BrowserScreen`(`react-native-webview`) 사용.

### 7.5 CategoryChips 폭 점프 수정

활성 칩에만 `fontWeight: 700`이 적용되어 가변 너비로 인해 활성/비활성 전환 시 칩 폭이 점프하던 문제 수정 (`frontend/src/components/CategoryChips.tsx`).

* chip에 `minWidth: 64` + `alignItems: 'center'` 추가
* 라벨에 `textAlign: 'center'`, 비활성도 `fontWeight: '600'`으로 통일하여 폭 안정화

### 7.6 자산(이미지) 임시 처리

`assets/icon.png`, `assets/splash.png`, `assets/adaptive-icon.png` 파일이 부재하여 번들 경고 발생. `app.json`에서 해당 참조를 제거하고 Expo 기본 자산을 사용 중. 정식 배포 전 자체 자산 추가 필요.

### 7.7 다음 단계 (Open Issues)

* 실제 자산(아이콘, 스플래시, 어댑티브 아이콘) 추가
* 콘솔 에러 / 텍스트 줄임 위치 추가 점검 (사용자 보고 대기)
* `react-native-webview`가 웹에서 외부 사이트 X-Frame-Options에 막히는 경우의 `BrowserScreen` 폴백 강화
* Sentry 정식 도입 시 `@sentry/react-native`로 마이그레이션
* `frontend/app.json`의 `API_BASE_URL` 자동 감지(개발 모드에서 호스트 IP 기반)
* 방화벽 인바운드 4000/8081 룰 자동화 또는 README 안내 강화