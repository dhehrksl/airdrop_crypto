# 실행 방법

다른 컴퓨터에서 받은 직후 환경 설정만 하고 바로 실행하기 위한 가이드.

---

## 1. 사전 요구사항

| 항목 | 권장 버전 | 비고 |
| :--- | :--- | :--- |
| Node.js | **≥ 20.19.4** | Metro 0.83.x가 요구. v20.13.x는 동작은 하나 EBADENGINE 경고 |
| npm | ≥ 10 | Node에 포함 |
| MongoDB | 7.x 로컬 또는 Atlas | 백엔드가 시작 시 연결 |
| Git | 최신 | clone 용 |
| (선택) Expo Go 앱 | 폰에 설치 | 모바일 실기기 테스트 시 SDK 54 호환 |

운영체제: Windows / macOS / Linux 모두 지원. 아래 예시는 PowerShell(Windows) 기준이며, macOS/Linux는 `copy` 대신 `cp` 사용.

---

## 2. 클론 및 의존성 설치

```powershell
git clone https://github.com/dhehrksl/airdrop_crypto.git
cd airdrop_crypto

# 백엔드 / 관리자 / 모바일 의존성 한 번씩 설치
cd backend  ; npm install ; cd ..
cd admin    ; npm install ; cd ..
cd frontend ; npm install ; cd ..
```

> `frontend`에는 `.npmrc`(`legacy-peer-deps=true`)가 커밋되어 있어 SDK 54 peer-dep 충돌이 자동 회피됩니다.

---

## 3. 환경 설정

### 3.1 백엔드 (`backend/.env`)

```powershell
copy backend\.env.example backend\.env
```

기본값(`USE_MOCK=true`)이면 **외부 API 호출 없이 mock 샘플 데이터로 동작**합니다. 정말 최소 구성으로 돌리려면 다음만 확인하면 끝:

| 키 | 기본 | 변경 시점 |
| :--- | :--- | :--- |
| `MONGO_URI` | `mongodb://127.0.0.1:27017/airdrop_crypto` | Atlas 쓰면 변경 |
| `USE_MOCK` | `true` | 실제 Gemini 호출 시 `false` |
| `GEMINI_API_KEY` | (빈 값) | `USE_MOCK=false` 시 필요 |
| `ADMIN_TOKEN` | `changeme_admin_token` | 관리자 대시보드 사용 시 임의 강한 값으로 변경 |

### 3.2 관리자 (`admin/.env`)

```powershell
copy admin\.env.example admin\.env
```

기본 `VITE_API_BASE_URL=http://localhost:4000`이라 백엔드를 같은 PC에서 띄우면 수정 불필요.

### 3.3 모바일 (`frontend/app.json`)

> 이 항목은 `.env`가 아니라 **`app.json`의 `extra.API_BASE_URL`** 값입니다. (Expo는 빌드 시점에 `app.json`을 읽음)

폰 실기기 테스트나 다른 PC의 백엔드를 가리키려면 `frontend/app.json`을 열고 다음을 본인 PC의 LAN IP로 수정:

```json
"extra": {
  "API_BASE_URL": "http://192.168.x.x:4000",
  ...
}
```

PC LAN IP 확인 방법 (Windows PowerShell):

```powershell
Get-NetIPAddress -AddressFamily IPv4 -PrefixOrigin Dhcp,Manual `
  | Where-Object { $_.IPAddress -notlike '127.*' -and $_.IPAddress -notlike '169.254.*' } `
  | Select-Object IPAddress, InterfaceAlias
```

---

## 4. MongoDB 띄우기

이미 설치되어 있고 서비스로 등록된 경우 자동 실행됩니다. 그렇지 않으면:

```powershell
# 로컬 설치본
mongod --dbpath <db경로>

# 또는 Docker
docker run -d -p 27017:27017 --name airdrop-mongo mongo:7
```

연결 확인:

```powershell
Test-NetConnection 127.0.0.1 -Port 27017
```

---

## 5. 실행 (3개 터미널)

각 서비스를 **별도 PowerShell 창**에서 띄웁니다.

### 5.1 백엔드 — 터미널 #1

```powershell
cd backend
npm run seed   # 최초 1회: mock 데이터 10건 시딩
npm start      # http://localhost:4000
```

확인: `Invoke-WebRequest http://localhost:4000/health` → `{"ok":true,...}`

### 5.2 관리자 대시보드 — 터미널 #2

```powershell
cd admin
npm run dev    # http://localhost:5173
```

브라우저에서 열고 `ADMIN_TOKEN` 값 입력 → 통계 차트 표시 (30초 자동 갱신).

### 5.3 모바일 (Expo) — 터미널 #3

```powershell
cd frontend
npx expo start --lan
```

콘솔에 QR과 `exp://<LAN-IP>:8081` 주소가 표시됩니다.

---

## 6. 폰에서 앱 띄우기

1. 폰을 PC와 **같은 Wi-Fi**에 연결
2. Expo Go 앱(SDK 54용)을 Play Store / App Store에서 설치
3. Expo Go에서 QR 스캔 또는 "Enter URL manually"에 `exp://<LAN-IP>:8081` 입력

처음 로딩 시 번들링이 1~2분 걸릴 수 있습니다.

### 6.1 Windows 방화벽 인바운드 허용 (필요 시 1회)

폰에서 `Network response timed out`이 뜨면 아래 명령을 **관리자 PowerShell**에서 실행:

```powershell
New-NetFirewallRule -DisplayName "Airdrop Expo+Backend" `
  -Direction Inbound -LocalPort 4000,8081 -Protocol TCP -Action Allow
```

---

## 7. PC 브라우저에서 띄우기 (선택)

폰 없이 빠르게 동작 확인하고 싶을 때:

```powershell
cd frontend
npx expo start --web
```

브라우저 자동 오픈 또는 http://localhost:8081 직접 접속.

> 일부 RN 컴포넌트(`react-native-webview` 등)는 웹에서 X-Frame-Options 등으로 외부 사이트를 보여주지 못할 수 있습니다. 참여 버튼은 새 탭에서 외부 사이트를 열도록 처리되어 있습니다.

---

## 8. 트러블슈팅

| 증상 | 원인 / 해결 |
| :--- | :--- |
| `npm install`이 ERESOLVE로 실패 | `frontend/.npmrc`가 누락된 경우. 파일에 `legacy-peer-deps=true` 한 줄이 있어야 함 |
| `Cannot find module 'babel-preset-expo'` | `cd frontend ; npm install --save-dev babel-preset-expo` |
| Expo Go: `Project is incompatible with this version of Expo Go` | 폰 Expo Go가 다른 SDK용. 본 프로젝트는 SDK 54 — Expo Go도 SDK 54로 맞추거나 프로젝트를 그 SDK로 다시 마이그레이션 |
| `failed to start tunnel ... remote gone away` | Expo 공용 ngrok 터널 일시 장애. `--lan`으로 사용 |
| 폰에서 "Network response timed out" | 방화벽 4000/8081 인바운드 또는 `app.json`의 `API_BASE_URL`이 잘못된 IP |
| 한글이 PowerShell에서 깨짐 | `chcp 65001 ; [Console]::OutputEncoding = [System.Text.Encoding]::UTF8` |
| `EBADENGINE ... Node v20.x.x` 경고 | Metro가 ≥20.19.4를 권장. 동작은 하지만 가능하면 Node 업그레이드 |

---

## 9. 한 번에 다 띄우기 (선택, 별칭)

세 터미널을 자동으로 여는 PowerShell 한 줄:

```powershell
Start-Process powershell -ArgumentList "-NoExit","-Command","cd backend; npm start"
Start-Process powershell -ArgumentList "-NoExit","-Command","cd admin; npm run dev"
Start-Process powershell -ArgumentList "-NoExit","-Command","cd frontend; npx expo start --lan"
```

각각 새 창에서 띄워지므로 로그가 섞이지 않습니다.
